<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= page.title %> - Valorant Wiki</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
    }
    .container {
      max-width: 1000px;
      margin: 0 auto;
      background: white;
      min-height: 100vh;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
    }
    .header {
      background: #ff4655;
      color: white;
      padding: 20px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 2.5em;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }
    .meta {
      margin: 10px 0 0 0;
      opacity: 0.9;
      font-size: 0.9em;
    }
    .content {
      padding: 30px;
      line-height: 1.6;
      font-size: 16px;
    }
    .content h2 {
      color: #ff4655;
      border-bottom: 2px solid #ff4655;
      padding-bottom: 10px;
      margin-top: 30px;
    }
    .content h3 {
      color: #333;
      margin-top: 25px;
    }
    .content ul {
      margin: 15px 0;
    }
    .content li {
      margin: 8px 0;
    }
    .footer {
      background: #f8f9fa;
      padding: 20px;
      text-align: center;
      border-top: 1px solid #dee2e6;
    }
    .edit-btn {
      background: #ff4655;
      color: white;
      padding: 12px 24px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: bold;
      transition: background 0.3s;
    }
    .edit-btn:hover {
      background: #e03e4c;
    }
    .back-btn {
      background: #6c757d;
      color: white;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 5px;
      margin-right: 10px;
    }
    .back-btn:hover {
      background: #5a6268;
    }
    .nav-buttons {
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><%= page.title %></h1>
      <p class="meta">
        📅 Cập nhật lần cuối: <%= new Date(page.updated_at).toLocaleString('vi-VN') %> | 
        📂 <%= page.category %> | 
        🔗 /<%= page.category %>/<%= page.slug %>
      </p>
    </div>

    <div class="nav-buttons" style="padding: 20px 30px 0;">
      <a href="/wiki" class="back-btn">📚 Danh sách trang</a>
      <a href="/dashboard.html" class="back-btn">🏠 Dashboard</a>
    </div>

    <div class="content">
      <%- page.content %>
    </div>

    <div class="footer">
      <a href="/wiki/edit/<%= page.page_id %>" class="edit-btn">✏️ Chỉnh sửa trang này</a>
    </div>
  </div>
</body>
</html>
